CREATE TABLE [dbo].[Ingredients] (
    [idIngredient]       INT          NOT NULL,
    [strIngredient]      VARCHAR (30) NOT NULL,
    [strIngredientThumb] VARCHAR (85) NOT NULL,
    PRIMARY KEY CLUSTERED ([idIngredient] ASC)
);

